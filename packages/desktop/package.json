{
  "name": "@openkanban/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "bundle:sidecar": "npx esbuild ../../packages/sidecar/src/bundle-entry.ts --bundle --platform=node --format=cjs --outfile=../../packages/sidecar/dist/sidecar-bundle.cjs",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "svelte": "^5.0.0",
    "vite": "^6.0.0"
  }
}
